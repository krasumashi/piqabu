node_modules/
.expo/
dist/
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.env
.env.local
.env.development.local
.env.test.local
.env.production.local
server/data/

# Release artifacts — scripts/release-apk.sh downloads the EAS build
# into this filename then attaches it to the GitHub release. If a
# release fails partway, the file may be left behind — must not be
# committed.
piqabu.apk
*.apk
